<h1>{L_STYLE_EXPORTER}</h1>
<p>{L_EXPORTER_EXPLAIN}</p>

<form method="post" action="{S_EXPORTER_ACTION}" class="centered">
	<label>{L_TEMPLATE_SELECT}</label>
	{S_TEMPLATE_SELECT}
	<input type="submit" name="edit" value="{L_SUBMIT}" class="btn btn-primary" />
</form>